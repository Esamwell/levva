# dependências
node_modules/

# build
.next/
out/

# ambiente — NUNCA versionar segredos (JWT_SECRET, senha do banco, token WhatsApp)
.env
.env.local
.env.production

# documentos enviados por motoristas (dado pessoal, nunca versionar)
uploads/
public/uploads/

# prisma
prisma/*.db

# config do nginx gerada pelo instalador (ver nginx/*.conf)
nginx.active.conf

# typescript (gerado pelo Next a cada build)
next-env.d.ts

# diversos
.DS_Store
*.log
npm-debug.log*
*.tsbuildinfo
